--- Tunnel/Models/FakeCallConfig.swift
+++ Tunnel/Models/FakeCallConfig.swift
@@ -5,7 +5,6 @@
         static let contactName = "Contact"
         static let contactSubtitle = "mobile"
         static let fakePhoneNumber = "+33 6 00 00 00 00"
-        static let delaySeconds = 0
         static let ringtoneName = "default_ringtone"
         static let useSlideToAnswer = false
     }
@@ -13,7 +12,6 @@
     var contactName: String = Defaults.contactName
     var contactSubtitle: String = Defaults.contactSubtitle
     var fakePhoneNumber: String = Defaults.fakePhoneNumber
-    var delaySeconds: Int = Defaults.delaySeconds
     var ringtoneName: String = Defaults.ringtoneName
     var useSlideToAnswer: Bool = Defaults.useSlideToAnswer
     var contactImageName: String?
--- Tunnel/Models/AppState.swift
+++ Tunnel/Models/AppState.swift
@@ -21,7 +21,6 @@
         }
     }
 
-    private let scheduler = FakeCallScheduler()
     private let ringtonePlayer = RingtonePlayer()
     private let hapticsManager = HapticsManager()
     private var hasSeenOnboarding: Bool
@@ -35,20 +34,10 @@
 
     func triggerFakeCallNow() {
         guard screen != .incomingCall else { return }
-        scheduler.cancel()
         screen = .incomingCall
         startIncomingFeedback()
     }
 
-    func scheduleFakeCall(delay: TimeInterval) {
-        guard screen == .home else { return }
-
-        if delay <= 0 {
-            triggerFakeCallNow()
-            return
-        }
-
-        scheduler.schedule(delay: delay) { [weak self] in
-            self?.triggerFakeCallNow()
-        }
-    }
-
     func answerCall() {
         guard screen == .incomingCall else { return }
@@ -57,18 +46,15 @@
     }
 
     func endCall() {
-        scheduler.cancel()
         stopIncomingFeedback()
         screen = .home
     }
 
     func goHome() {
-        scheduler.cancel()
         stopIncomingFeedback()
         screen = .home
     }
 
     func openSettings() {
-        scheduler.cancel()
         stopIncomingFeedback()
         screen = .settings
     }
@@ -75,7 +61,6 @@
     }
 
     func openOnboarding() {
-        scheduler.cancel()
         stopIncomingFeedback()
         screen = .onboarding
     }
@@ -102,7 +87,6 @@
         UserDefaults.standard.set(config.contactName, forKey: StorageKeys.contactName)
         UserDefaults.standard.set(config.contactSubtitle, forKey: StorageKeys.contactSubtitle)
         UserDefaults.standard.set(config.fakePhoneNumber, forKey: StorageKeys.fakePhoneNumber)
-        UserDefaults.standard.set(config.delaySeconds, forKey: StorageKeys.delaySeconds)
         UserDefaults.standard.set(config.ringtoneName, forKey: StorageKeys.ringtoneName)
         UserDefaults.standard.set(config.useSlideToAnswer, forKey: StorageKeys.useSlideToAnswer)
         UserDefaults.standard.set(config.contactImageName, forKey: StorageKeys.contactImageName)
@@ -111,20 +95,12 @@
     private static func loadConfig() -> FakeCallConfig {
         let defaults = UserDefaults.standard
-        let storedDelay = defaults.object(forKey: StorageKeys.delaySeconds) as? Int
-        let normalizedDelay: Int
-        if let storedDelay, FakeCallDelayOption.allSeconds.contains(storedDelay) {
-            normalizedDelay = storedDelay
-        } else {
-            normalizedDelay = FakeCallConfig.Defaults.delaySeconds
-        }
 
         return FakeCallConfig(
             contactName: defaults.string(forKey: StorageKeys.contactName) ?? FakeCallConfig.Defaults.contactName,
             contactSubtitle: defaults.string(forKey: StorageKeys.contactSubtitle) ?? FakeCallConfig.Defaults.contactSubtitle,
             fakePhoneNumber: defaults.string(forKey: StorageKeys.fakePhoneNumber) ?? FakeCallConfig.Defaults.fakePhoneNumber,
-            delaySeconds: normalizedDelay,
             ringtoneName: defaults.string(forKey: StorageKeys.ringtoneName) ?? FakeCallConfig.Defaults.ringtoneName,
             useSlideToAnswer: defaults.object(forKey: StorageKeys.useSlideToAnswer) as? Bool ?? FakeCallConfig.Defaults.useSlideToAnswer,
             contactImageName: defaults.string(forKey: StorageKeys.contactImageName)
@@ -137,7 +113,6 @@
     static let contactName = "config.contactName"
     static let contactSubtitle = "config.contactSubtitle"
     static let fakePhoneNumber = "config.fakePhoneNumber"
-    static let delaySeconds = "config.delaySeconds"
     static let ringtoneName = "config.ringtoneName"
     static let useSlideToAnswer = "config.useSlideToAnswer"
     static let contactImageName = "config.contactImageName"
--- Tunnel/Views/HomeView.swift
+++ Tunnel/Views/HomeView.swift
@@ -17,29 +17,6 @@
                 }
                 .padding(.top, 24)
 
-                VStack(alignment: .leading, spacing: 12) {
-                    Text("Dans combien de temps ?")
-                        .font(.headline)
-
-                    Picker("Délai", selection: $appState.config.delaySeconds) {
-                        ForEach(FakeCallDelayOption.allSeconds, id: \.self) { delay in
-                            Text(localizedDelayLabel(delay)).tag(delay)
-                        }
-                    }
-                    .pickerStyle(.segmented)
-                }
-
-                Button("Programmer un faux appel") {
-                    appState.scheduleFakeCall(delay: TimeInterval(appState.config.delaySeconds))
-                }
-                .buttonStyle(.bordered)
-                .controlSize(.large)
-                .frame(maxWidth: .infinity, alignment: .leading)
-
                 Button("Guide Back Tap") {
                     appState.openOnboarding()
                 }
@@ -52,7 +29,7 @@
                     Button("Lancer un faux appel maintenant") {
                         appState.triggerFakeCallNow()
                     }
-                    .buttonStyle(.borderedProminent)
+                    .buttonStyle(LargeActionButtonStyle())
                     .controlSize(.large)
                     .frame(maxWidth: .infinity)
 
@@ -68,17 +45,21 @@
             .padding(20)
         }
     }
-
-    private func localizedDelayLabel(_ value: Int) -> String {
-        switch value {
-        case 0: return "Tout de suite"
-        case 3: return "3 sec"
-        case 5: return "5 sec"
-        case 10: return "10 sec"
-        case 30: return "30 sec"
-        case 60: return "1 min"
-        default: return "\(value) sec"
-        }
-    }
 }
 
+struct LargeActionButtonStyle: ButtonStyle {
+    func makeBody(configuration: Configuration) -> some View {
+        configuration.label
+            .font(.headline)
+            .padding(.vertical, 16)
+            .frame(maxWidth: .infinity)
+            .background(Color.accentColor)
+            .foregroundStyle(.white)
+            .clipShape(RoundedRectangle(cornerRadius: 16, style: .continuous))
+            .scaleEffect(configuration.isPressed ? 0.96 : 1.0)
+            .opacity(configuration.isPressed ? 0.9 : 1.0)
+            .animation(.spring(response: 0.2, dampingFraction: 0.7), value: configuration.isPressed)
+    }
+}
--- Tunnel/Views/SettingsView.swift
+++ Tunnel/Views/SettingsView.swift
@@ -15,14 +15,7 @@
                 }
 
-                Section("Déclenchement") {
-                    Picker("Délai par défaut", selection: $appState.config.delaySeconds) {
-                        ForEach(FakeCallDelayOption.allSeconds, id: \.self) { delay in
-                            Text(FakeCallDelayOption.label(for: delay)).tag(delay)
-                        }
-                    }
-
+                Section("Comportement") {
                     Toggle("Répondre avec \"Glisser pour répondre\"", isOn: $appState.config.useSlideToAnswer)
                 }
 
--- Tunnel/Intents/TriggerTunnelIntent.swift
+++ Tunnel/Intents/TriggerTunnelIntent.swift
@@ -5,12 +5,9 @@
     static var description = IntentDescription("Lance immédiatement un faux appel entrant.")
     static var openAppWhenRun: Bool = true
 
-    @Parameter(title: "Délai (secondes)", default: 0)
-    var delay: Int
-
     @MainActor
     func perform() async throws -> some IntentResult {
-        AppState.shared.scheduleFakeCall(delay: TimeInterval(delay))
+        AppState.shared.triggerFakeCallNow()
         return .result()
     }
 }
